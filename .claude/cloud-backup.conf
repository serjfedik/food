# Настройки бэкапа-хука. Меняй под проект.
INTERVAL_HOURS=24      # как часто реально делать бэкап из хука (в часах)
RETENTION_DAYS=30      # сколько дней хранить snapshot-теги
# Офсайт-зеркало (опционально): пустой репо на втором хосте + URL с токеном.
# MIRROR_REMOTE="https://oauth2:<PERSONAL_ACCESS_TOKEN>@gitlab.com/<login>/<repo>.git"
MIRROR_REMOTE=""
